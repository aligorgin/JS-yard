export default function Basics() {
    const text = 'hey';
    const text2 = 'wasap'
    console.log(`${text} ${text2}`);

    const a = 'hello';
    const b = 42;
    const c = {d: [true, false]}
    console.log({a, b, c})

    return (
        <div>
            open consolo
        </div>
    )
}